Author: Juan Valenzuela

This shell works in executing built-in commands cd, pwc, and exit. 
It can execute simple non-built-in unix commands. 
Redirection is supported but requires spaces between inputs and redirection symbols.
Error handling is also supported.
